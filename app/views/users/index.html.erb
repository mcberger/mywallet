<h1> All users </h1>

<ul>
	<% @users.each do |user| %>
		<li> <%= user_link user %> 
		<%= link_to "Delete", user_path(user), method: :delete, remote: true, data: {confirm: "Are you sure you want to delete this user?"}, class: "delete-user" %> </li>
		<br>
	<% end %>
</ul>